version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/html/myapp
    overwrite: true

hooks:
  BeforeInstall:
    - location: scripts/test.sh
      runas: root
  AfterInstall:
    - location: scripts/test.sh
      runas: root
